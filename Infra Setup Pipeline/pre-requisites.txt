1. Need a user named tomcatdevops on the respective server with sshd configurred for user login
2. Need a directory named digital on respective path /app/digital with ownership tomcatdevops:tomcatdevops
3. 





Variables 
-> /bin/catalina.sh

JAVA_OPTS="-server -XX:+UseG1GC -XX:+ExplicitGCInvokesConcurrent -XX:MaxGCPauseMillis=500 -Xms@heap.min@M -Xmx@heap.max@M -Xlog:gc*,safepoint=info:file=@tomcat.path@/apache-tomcat-@tomcat.version@-@tomcat.client@/logs/gc_%p_%t.log:time,uptime:filecount=10 -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=@tomcat.path@/apache-tomcat-@tomcat.version@-@tomcat.client@/heapdumps"

@heap.min@ = 512
@heap.max@ = 4096
@tomcat.path@ = /home/gourav/tomcat
@tomcat.version@ = 9.0.91
@tomcat.client@ = gourav

-> /conf/server.xml

<Connector port="@tomcat.port@" protocol="HTTP/1.1"
               connectionTimeout="20000" maxPostSize="209715200" 
               redirectPort="@tomcat.httpsport@" maxThreads="1500"/>

@tomcat.port@ = 8888
@tomcat.httpsport@ = 8443

-> /conf/tomcat-users.xml
<user username="@tomcat.usrname@" password="@tomcat.passwd@" roles="manager-gui" />
<user username="devops" password="devops@8881" roles="manager-gui,manager-script,manager-jmx,manager-status,admin-gui,admin-script"/>

@tomcat.usrname@ = gourav
@tomcat.passwd@ = go4it*11