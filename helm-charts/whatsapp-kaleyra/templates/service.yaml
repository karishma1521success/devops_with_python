apiVersion: v1
kind: Service
metadata:
  name: {{ include "whatsapp-kaleyra.fullname" . }}
  labels:
    {{- include "whatsapp-kaleyra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "whatsapp-kaleyra.selectorLabels" . | nindent 4 }}
