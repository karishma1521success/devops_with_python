apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "whatsapp-kaleyra.fullname" . }}-test-connection"
  labels:
    {{- include "whatsapp-kaleyra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "whatsapp-kaleyra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
