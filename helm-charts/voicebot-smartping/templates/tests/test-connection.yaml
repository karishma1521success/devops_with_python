apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "voicebot-smartping.fullname" . }}-test-connection"
  labels:
    {{- include "voicebot-smartping.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "voicebot-smartping.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
