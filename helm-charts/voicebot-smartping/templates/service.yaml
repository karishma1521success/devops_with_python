apiVersion: v1
kind: Service
metadata:
  name: {{ include "voicebot-smartping.fullname" . }}
  labels:
    {{- include "voicebot-smartping.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "voicebot-smartping.selectorLabels" . | nindent 4 }}
