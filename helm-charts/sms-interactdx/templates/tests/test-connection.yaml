apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sms-interactdx.fullname" . }}-test-connection"
  labels:
    {{- include "sms-interactdx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sms-interactdx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
