apiVersion: v1
kind: Service
metadata:
  name: {{ include "sms-interactdx.fullname" . }}
  labels:
    {{- include "sms-interactdx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sms-interactdx.selectorLabels" . | nindent 4 }}
