apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sms-ozonetel.fullname" . }}-test-connection"
  labels:
    {{- include "sms-ozonetel.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sms-ozonetel.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
