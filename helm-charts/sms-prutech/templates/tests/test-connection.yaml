apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sms-prutech.fullname" . }}-test-connection"
  labels:
    {{- include "sms-prutech.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sms-prutech.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
