apiVersion: v1
kind: Service
metadata:
  name: {{ include "sms-prutech.fullname" . }}
  labels:
    {{- include "sms-prutech.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sms-prutech.selectorLabels" . | nindent 4 }}
