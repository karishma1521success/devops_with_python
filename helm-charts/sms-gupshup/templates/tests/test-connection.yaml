apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sms-gupshup.fullname" . }}-test-connection"
  labels:
    {{- include "sms-gupshup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sms-gupshup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
