apiVersion: v1
kind: Service
metadata:
  name: {{ include "sms-gupshup.fullname" . }}
  labels:
    {{- include "sms-gupshup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sms-gupshup.selectorLabels" . | nindent 4 }}
