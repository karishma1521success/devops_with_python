apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "voice-valuefirst.fullname" . }}-test-connection"
  labels:
    {{- include "voice-valuefirst.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "voice-valuefirst.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
