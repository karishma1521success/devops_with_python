apiVersion: v1
kind: Service
metadata:
  name: {{ include "voice-valuefirst.fullname" . }}
  labels:
    {{- include "voice-valuefirst.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "voice-valuefirst.selectorLabels" . | nindent 4 }}
