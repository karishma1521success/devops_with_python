apiVersion: v1
kind: Service
metadata:
  name: {{ include "voice-ozonetel.fullname" . }}
  labels:
    {{- include "voice-ozonetel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "voice-ozonetel.selectorLabels" . | nindent 4 }}
