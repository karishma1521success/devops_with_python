apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "voice-ozonetel.fullname" . }}-test-connection"
  labels:
    {{- include "voice-ozonetel.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "voice-ozonetel.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
