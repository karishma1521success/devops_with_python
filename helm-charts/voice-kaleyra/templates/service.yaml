apiVersion: v1
kind: Service
metadata:
  name: {{ include "voice-kaleyra.fullname" . }}
  labels:
    {{- include "voice-kaleyra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "voice-kaleyra.selectorLabels" . | nindent 4 }}
