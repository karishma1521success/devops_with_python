apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "voice-kaleyra.fullname" . }}-test-connection"
  labels:
    {{- include "voice-kaleyra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "voice-kaleyra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
