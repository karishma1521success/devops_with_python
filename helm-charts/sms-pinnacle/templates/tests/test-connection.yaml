apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sms-pinnacle.fullname" . }}-test-connection"
  labels:
    {{- include "sms-pinnacle.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sms-pinnacle.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
