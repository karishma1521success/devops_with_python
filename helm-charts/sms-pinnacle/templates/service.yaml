apiVersion: v1
kind: Service
metadata:
  name: {{ include "sms-pinnacle.fullname" . }}
  labels:
    {{- include "sms-pinnacle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sms-pinnacle.selectorLabels" . | nindent 4 }}
