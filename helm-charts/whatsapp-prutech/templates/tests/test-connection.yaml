apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "whatsapp-prutech.fullname" . }}-test-connection"
  labels:
    {{- include "whatsapp-prutech.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "whatsapp-prutech.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
