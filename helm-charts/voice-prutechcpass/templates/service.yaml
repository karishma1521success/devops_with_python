apiVersion: v1
kind: Service
metadata:
  name: {{ include "voice-prutechcpass.fullname" . }}
  labels:
    {{- include "voice-prutechcpass.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "voice-prutechcpass.selectorLabels" . | nindent 4 }}
