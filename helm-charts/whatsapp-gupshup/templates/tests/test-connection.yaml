apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "whatsapp-gupshup.fullname" . }}-test-connection"
  labels:
    {{- include "whatsapp-gupshup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "whatsapp-gupshup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
