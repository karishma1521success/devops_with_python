apiVersion: v1
kind: Service
metadata:
  name: {{ include "whatsapp-gupshup.fullname" . }}
  labels:
    {{- include "whatsapp-gupshup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "whatsapp-gupshup.selectorLabels" . | nindent 4 }}
