apiVersion: v1
kind: Service
metadata:
  name: {{ include "voicebot-verbalyze.fullname" . }}
  labels:
    {{- include "voicebot-verbalyze.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "voicebot-verbalyze.selectorLabels" . | nindent 4 }}
