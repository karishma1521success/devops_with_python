apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "voicebot-verbalyze.fullname" . }}-test-connection"
  labels:
    {{- include "voicebot-verbalyze.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "voicebot-verbalyze.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
