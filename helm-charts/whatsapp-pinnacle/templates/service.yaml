apiVersion: v1
kind: Service
metadata:
  name: {{ include "whatsapp-pinnacle.fullname" . }}
  labels:
    {{- include "whatsapp-pinnacle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "whatsapp-pinnacle.selectorLabels" . | nindent 4 }}
