apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "whatsapp-pinnacle.fullname" . }}-test-connection"
  labels:
    {{- include "whatsapp-pinnacle.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "whatsapp-pinnacle.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
