apiVersion: v1
kind: Service
metadata:
  name: {{ include "voice-prutech.fullname" . }}
  labels:
    {{- include "voice-prutech.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "voice-prutech.selectorLabels" . | nindent 4 }}
