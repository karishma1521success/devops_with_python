apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "voice-prutech.fullname" . }}-test-connection"
  labels:
    {{- include "voice-prutech.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "voice-prutech.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
