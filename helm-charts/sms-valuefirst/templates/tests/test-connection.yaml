apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sms-valuefirst.fullname" . }}-test-connection"
  labels:
    {{- include "sms-valuefirst.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sms-valuefirst.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
