apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sms-kaleyra.fullname" . }}-test-connection"
  labels:
    {{- include "sms-kaleyra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sms-kaleyra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
