apiVersion: v1
kind: Service
metadata:
  name: {{ include "rcs-prutech.fullname" . }}
  labels:
    {{- include "rcs-prutech.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rcs-prutech.selectorLabels" . | nindent 4 }}
