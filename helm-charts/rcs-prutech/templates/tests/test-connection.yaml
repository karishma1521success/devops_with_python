apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rcs-prutech.fullname" . }}-test-connection"
  labels:
    {{- include "rcs-prutech.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rcs-prutech.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
